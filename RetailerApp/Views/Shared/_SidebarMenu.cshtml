@using BOTS_BL.Models;
@using RetailerApp.App_Start;
@{

    var userDetails = (CustomerLoginDetail)Session["UserSession"];

    var roleId = userDetails.LoginType;
    var level = userDetails.LevelIndicator;
    var gId = userDetails.GroupId;
}
<div class="app-sidebar sidebar-shadow">
    <div class="app-header__logo">
        <div class="logo-src"></div>
        <div class="header__pane ml-auto">
            <div>
                <button type="button" class="hamburger close-sidebar-btn hamburger--elastic" data-class="closed-sidebar">
                    <span class="hamburger-box">
                        <span class="hamburger-inner"></span>
                    </span>
                </button>
            </div>
        </div>
    </div>
    <div class="app-header__mobile-menu">
        <div>
            <button type="button" class="hamburger hamburger--elastic mobile-toggle-nav">
                <span class="hamburger-box">
                    <span class="hamburger-inner"></span>
                </span>
            </button>
        </div>
    </div>
    <div class="app-header__menu">
        <span>
            <button type="button" class="btn-icon btn-icon-only btn btn-primary btn-sm mobile-toggle-header-nav">
                <span class="btn-icon-wrapper">
                    <i class="fa fa-ellipsis-v fa-w-6"></i>
                </span>
            </button>
        </span>
    </div>
    <div class="scrollbar-sidebar">
        <div class="app-sidebar__inner">
            <ul class="vertical-nav-menu">
                <li class="@Html.IsSelected(controller: "Home", action: "Index")">
                    <a class="@Html.IsSelected(controller: "Home", action: "Index")" href="@Url.Action("Index", "Home")">
                        <i class="metismenu-icon"></i>
                        Search Customer
                    </a>
                </li>
                <li class="@Html.IsSelected(controller: "Home", action: "DailySummaryReport")">
                    <a class="@Html.IsSelected(controller: "Home", action: "DailySummaryReport")" href="@Url.Action("DailySummaryReport", "Home")">
                        <i class="metismenu-icon"></i>
                        Daily Summary Report
                    </a>
                </li>
                <li class="@Html.IsSelected(controller: "Home", action: "MemberDetailedReport")">
                    <a class="@Html.IsSelected(controller: "Home", action: "MemberDetailedReport")" href="@Url.Action("MemberDetailedReport", "Home")">
                        <i class="metismenu-icon"></i>
                        Member Detailed Report
                    </a>
                </li>
                <li class="@Html.IsSelected(controller: "Home", action: "CancelTxn")">
                    <a class="@Html.IsSelected(controller: "Home", action: "CancelTxn")" href="@Url.Action("CancelTxn", "Home")">
                        <i class="metismenu-icon"></i>
                        Cancel Txn
                    </a>
                </li>

            </ul>

        </div>
    </div>
</div>
