@using BOTS_BL.Models;
@{
    var userDetails = (CustomerLoginDetail)Session["UserSession"];
    var GroupId = userDetails.GroupId;
    var roleId = userDetails.LoginType;
    var level = userDetails.LevelIndicator;
}
<div class="app-sidebar sidebar-shadow">
    <div class="app-header__logo">
        <div class="logo-src"></div>
        <div class="header__pane ml-auto">
            <div>
                <button type="button" class="hamburger close-sidebar-btn hamburger--elastic" data-class="closed-sidebar">
                    <span class="hamburger-box">
                        <span class="hamburger-inner"></span>
                    </span>
                </button>
            </div>
        </div>
    </div>
    <div class="app-header__mobile-menu">
        <div>
            <button type="button" class="hamburger hamburger--elastic mobile-toggle-nav">
                <span class="hamburger-box">
                    <span class="hamburger-inner"></span>
                </span>
            </button>
        </div>
    </div>
    <div class="app-header__menu">
        <span>
            <button type="button" class="btn-icon btn-icon-only btn btn-primary btn-sm mobile-toggle-header-nav">
                <span class="btn-icon-wrapper">
                    <i class="fa fa-ellipsis-v fa-w-6"></i>
                </span>
            </button>
        </span>
    </div>
    <div class="scrollbar-sidebar">
        <div class="app-sidebar__inner">
            <ul class="vertical-nav-menu">
                <li>
                    <a href="@Url.Action("Index", "Lead")" class="@Html.IsSelected(controller: "Lead", action: "Index")">
                        <i class="metismenu-icon pe-7s-rocket"></i>
                        Home
                    </a>
                </li>
                @*<li>
                        <a href="@Url.Action("AddLead", "Lead")" class="@Html.IsSelected(controller: "Lead", action: "AddLead")">
                            <i class="metismenu-icon pe-7s-rocket"></i>
                            Add Lead
                        </a>
                    </li>*@
                @if (userDetails != null)
                {
                    if (userDetails.LoginType == "1" || userDetails.LoginType == "5")
                    {
                        <li>
                            <a href="@Url.Action("LeadTransfer", "Lead")" class="@Html.IsSelected(controller: "Lead", action: "LeadTransfer")">
                                <i class="metismenu-icon pe-7s-rocket"></i>
                                Lead Transfer
                            </a>
                        </li>
                    }
                }
                <li>
                    <a href="@Url.Action("SalesCount", "Lead")" class="@Html.IsSelected(controller: "Lead", action: "SalesCount")">
                        <i class="metismenu-icon pe-7s-rocket"></i>
                        Sales Count
                    </a>
                </li>
                @if (userDetails != null)
                {
                    if (userDetails.LoginType == "1" || userDetails.LoginType == "5")
                    {
                        <li>
                            <a href="@Url.Action("MeetingMatrix", "LeadReport")" class="@Html.IsSelected(controller: "LeadReport", action: "MeetingMatrix")">
                                <i class="metismenu-icon pe-7s-rocket"></i>
                                Meeting Matrix
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("SalesMatrix", "LeadReport")" class="@Html.IsSelected(controller: "LeadReport", action: "SalesMatrix")">
                                <i class="metismenu-icon pe-7s-rocket"></i>
                                Sales Matrix
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("CallingMatrix", "LeadReport")" class="@Html.IsSelected(controller: "LeadReport", action: "CallingMatrix")">
                                <i class="metismenu-icon pe-7s-rocket"></i>
                                Calling/lead Matrix
                            </a>
                        </li>
                    }
                }
            </ul>
        </div>
    </div>
</div>
